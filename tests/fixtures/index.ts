/**
 * Test Fixtures Index
 *
 * Central export for all test fixtures.
 *
 * @example
 * ```ts
 * import { testUsers, testBookings } from '@/../tests/fixtures';
 *
 * describe('BookingService', () => {
 *   it('should create booking', () => {
 *     const user = testUsers.user;
 *     const booking = testBookings.pending;
 *     // ... test logic
 *   });
 * });
 * ```
 */

export * from "./bookings";
export * from "./users";
